import PageHeader from "@/components/PageHeader";

export default function DashboardPage() {
    return (
        <div>
            <PageHeader
                pageHeader={{
                    title: "Dashboard",
                    subTitle: "dashboard",
                }}
            />
            <h1>Dashboard</h1>
        </div>
    );
}
